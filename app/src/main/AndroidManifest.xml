<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spotifymatchmaker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Instr"></activity>
        <activity android:name=".SongS"></activity>

        <activity android:name=".Rock"></activity>
        <activity android:name=".Pop"></activity>
        <activity android:name=".Kpop"></activity>
        <activity android:name=".Rnb"></activity>
        <activity android:name=".Rap"></activity>


    </application>

</manifest>